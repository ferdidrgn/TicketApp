<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ferdidrgn.ticketapp">

    <!-- ================= Permissions ================= -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!--  Konum -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ================= Queries ================= -->

    <queries>
        <package android:name="com.google.android.gms" />

        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>

        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <!-- ================= Application ================= -->

    <application
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TiyatRol App"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <!-- ✅ AdMob App ID (TEK YER) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5779807348211992~7618602070" />

        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Google Maps API anahtarı: build.gradle, android/local.properties
        (gitignore'da, hiç commit edilmez) içindeki MAPS_API_KEY değerini
        manifestPlaceholders üzerinden buraya enjekte ediyor. -->
        <meta-data
            android:name="com.google.android.gms.maps.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <meta-data
            android:name="android.window.setDecorFitsSystemWindows"
            android:value="false" />

        <!-- ================= Main Activity ================= -->

        <activity
            android:name=".MainActivity"
            android:configChanges="
                orientation|
                keyboardHidden|
                keyboard|
                screenSize|
                smallestScreenSize|
                locale|
                layoutDirection|
                fontScale|
                screenLayout|
                density|
                uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter normal theme -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- 🚀 App Launcher Intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                🔗 HTTPS Deep Links (App Links - Auto-verified)
                Flutter web uygulaması artık domain KÖKÜNDE yayınlanıyor
                (bkz. scripts/deploy.sh, firebase.json rota-bazlı
                rewrite'lar -> /app-shell.html). Domain kökünün KENDİSİ
                ("/", "/privacy.html", "/terms.html" ...) hâlâ statik
                landing sitesi — bunlar uygulamaya değil tarayıcıya
                açılmalı, o yüzden TEK bir geniş pathPrefix yerine sadece
                gerçekten paylaşılabilir/uygulamada açılması gereken
                içerik rotaları (show/player/stage/team detay, biletlerim,
                kampanya detayı) tek tek eşleniyor — landing'in kendi
                sayfaları asla bu intent-filter'a girmiyor.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Production Domain — gerçek paylaşılabilir içerik rotaları -->
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/show" />
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/player" />
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/stage" />
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/team" />
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/my-tickets" />
                <data
                    android:host="tiyatrol.web.app"
                    android:scheme="https"
                    android:pathPrefix="/campaign-details" />
            </intent-filter>

            <!-- 🔗 Custom URL Scheme (Fallback) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="open"
                    android:scheme="tiyatrol" />
            </intent-filter>

        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>
</manifest>