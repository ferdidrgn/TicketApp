import 'package:flutter/material.dart';

class StageDetailPage extends StatelessWidget {
  final String stageId;

  const StageDetailPage({super.key, required this.stageId});

  @override
  Widget build(final BuildContext context) => throw UnsupportedError(
      'Cannot create StageDetailPage. Platform-specific code failed to load.');
}
